<div class="tab-content">
    <% @results.each do |key, result| %>
        <% show = ( key == 'photos' ) ? 'show' : '' %>
        <div class="tab-pane fade show active" id="<%= key %>" role="tabpanel" aria-labelledby="<%= key %>-tab">
            <%= key %>
        </div>
    <% end %>
</div>